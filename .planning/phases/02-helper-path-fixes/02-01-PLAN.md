---
phase: 02-helper-path-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - helpers/maxmind.go
  - helpers/threatIntel.go
autonomous: true

must_haves:
  truths:
    - "MaxMind glob patterns use filepath.Join so database discovery works on Linux"
    - "MaxMind download/extraction paths use filepath.Join so .tar.gz and .mmdb files are written correctly on Linux"
    - "Threat intel feed download paths use filepath.Join so feed files land in the intel directory on Linux"
    - "Threat intel ingestion paths use filepath.Join so downloaded feed files are read correctly on Linux"
    - "No hardcoded backslash path separators remain in maxmind.go or threatIntel.go"
  artifacts:
    - path: "helpers/maxmind.go"
      provides: "Cross-platform MaxMind database path handling"
      contains: "filepath.Join"
    - path: "helpers/threatIntel.go"
      provides: "Cross-platform threat intel path handling"
      contains: "filepath.Join"
  key_links:
    - from: "helpers/maxmind.go"
      to: "filepath.Join"
      via: "all path construction calls"
      pattern: "filepath\\.Join"
    - from: "helpers/threatIntel.go"
      to: "filepath.Join"
      via: "all path construction calls"
      pattern: "filepath\\.Join"
---

<objective>
Replace all hardcoded backslash path separators in MaxMind and threat intel helper modules with filepath.Join() calls, and remove resolved cross-platform TODO comments.

Purpose: MaxMind database discovery (glob patterns), download/extraction paths, and threat intel feed paths all use fmt.Sprintf with hardcoded backslashes. These break on Linux. Applying the same filepath.Join() pattern established in Phase 1 makes these modules cross-platform.

Output: Modified helpers/maxmind.go and helpers/threatIntel.go with zero hardcoded backslash path separators.
</objective>

<execution_context>
@C:\Users\Joe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Joe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@helpers/maxmind.go
@helpers/threatIntel.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace backslash paths in maxmind.go with filepath.Join</name>
  <files>helpers/maxmind.go</files>
  <action>
Replace all fmt.Sprintf path constructions using hardcoded backslashes with filepath.Join() calls. The file already imports "path/filepath". Specific changes:

1. Line 24 (FindOrGetDBs glob pattern):
   Change: `globPattern := fmt.Sprintf("%v\\Geo*.mmdb", dir)`
   To: `globPattern := filepath.Join(dir, "Geo*.mmdb")`

2. Line 63 (FindOrGetDBs log message -- cosmetic but use filepath.Join for consistency):
   Change: `logger.Info().Msgf("Could not find %v DB at %v\\%v, downloading!", k, dir, vars.MaxMindFiles[k])`
   To: `logger.Info().Msgf("Could not find %v DB at %v, downloading!", k, filepath.Join(dir, vars.MaxMindFiles[k]))`

3. Line 86 (updateMaxMind gzFile path):
   Change: `gzFile := fmt.Sprintf("%v\\%v.tar.gz", dir, k)`
   To: `gzFile := filepath.Join(dir, k+".tar.gz")`

4. Line 112 (updateMaxMind extracted mmdb glob pattern):
   Change: `globPattern := fmt.Sprintf("%v\\GeoLite2-%v_*\\GeoLite2-%v.mmdb", dir, k, k)`
   To: `globPattern := filepath.Join(dir, fmt.Sprintf("GeoLite2-%v_*", k), fmt.Sprintf("GeoLite2-%v.mmdb", k))`

5. Line 133 (updateMaxMind temp dir cleanup glob):
   Change: `tempDirPattern := fmt.Sprintf("%v\\GeoLite2-%v_*", dir, k)`
   To: `tempDirPattern := filepath.Join(dir, fmt.Sprintf("GeoLite2-%v_*", k))`

6. Remove all "TODO - Support Cross-Platform Compilation" comments in maxmind.go (lines 62, 85, 111, 132) -- the work they reference is now complete.

Note: The "fmt" import is still needed for Sprintf calls in glob sub-patterns and log messages, so do NOT remove it. The "path/filepath" import already exists.
  </action>
  <verify>
Run: `grep -n "\\\\" helpers/maxmind.go` -- should return zero matches (no hardcoded backslashes remain).
Run: `grep -n "filepath.Join" helpers/maxmind.go` -- should show 5 filepath.Join calls.
Run: `grep -n "TODO.*Cross-Platform" helpers/maxmind.go` -- should return zero matches.
Run: `CGO_ENABLED=0 go build .` -- should compile successfully.
  </verify>
  <done>All 5 backslash path constructions in maxmind.go replaced with filepath.Join(). All 4 cross-platform TODO comments removed. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace backslash paths in threatIntel.go with filepath.Join</name>
  <files>helpers/threatIntel.go</files>
  <action>
Replace all fmt.Sprintf path constructions using hardcoded backslashes with filepath.Join() calls. The file already imports "path/filepath". Specific changes:

1. Line 172 (updateIntelligence feed download dest):
   Change: `destFile := fmt.Sprintf("%v\\%v.txt", intelDir, feeds.Feeds[i].Name)`
   To: `destFile := filepath.Join(intelDir, feeds.Feeds[i].Name+".txt")`

2. Line 262 (ingestIntel commented-out dead code with backslash):
   Remove the entire commented-out line: `//err = IngestFile(fmt.Sprintf("%v\\%v", intelDir, e.Name()), ...)`
   This is dead code that was already commented out. Remove it entirely rather than fixing the backslash.

3. Line 264 (ingestIntel active IngestFile path):
   Change: `err = IngestFile(fmt.Sprintf("%v\\%v", intelDir, e.Name()), ...)`
   To: `err = IngestFile(filepath.Join(intelDir, e.Name()), ...)`
   Keep the rest of the arguments exactly the same.

4. Line 428 (IngestIPNetLists dest path):
   Change: `dest := fmt.Sprintf("%v\\%v", intelDir, file)`
   To: `dest := filepath.Join(intelDir, file)`

5. Remove all "TODO - Support Cross-Platform Compilation" comments in threatIntel.go (lines 171, 263, 427) -- the work they reference is now complete.

Note: The "fmt" import is still needed elsewhere in the file, do NOT remove it. The "path/filepath" import already exists.
  </action>
  <verify>
Run: `grep -n "\\\\" helpers/threatIntel.go` -- should return zero matches (no hardcoded backslashes remain).
Run: `grep -n "filepath.Join" helpers/threatIntel.go` -- should show 3 filepath.Join calls.
Run: `grep -n "TODO.*Cross-Platform" helpers/threatIntel.go` -- should return zero matches.
Run: `CGO_ENABLED=0 go build .` -- should compile successfully.
  </verify>
  <done>All 3 active backslash path constructions in threatIntel.go replaced with filepath.Join(). 1 dead-code line removed. All 3 cross-platform TODO comments removed. Build passes.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn "\\\\" helpers/maxmind.go helpers/threatIntel.go` returns zero results
2. `grep -rn "TODO.*Cross-Platform" helpers/maxmind.go helpers/threatIntel.go` returns zero results
3. `CGO_ENABLED=0 go build .` compiles successfully
4. Requirements DBPATH-01, DBPATH-02, DBPATH-03 are satisfied
</verification>

<success_criteria>
- Zero hardcoded backslash path separators in helpers/maxmind.go
- Zero hardcoded backslash path separators in helpers/threatIntel.go
- Zero cross-platform TODO comments in either file
- Build compiles with CGO_ENABLED=0
- filepath.Join used for all path construction in both files
</success_criteria>

<output>
After completion, create `.planning/phases/02-helper-path-fixes/02-01-SUMMARY.md`
</output>
