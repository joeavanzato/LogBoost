---
phase: 02-helper-path-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - helpers/helpers.go
autonomous: true

must_haves:
  truths:
    - "ExtractTarGz writes extracted directories and files to correct paths on Linux"
    - "CombineOutputs creates combined CSV output files at correct paths on Linux"
    - "Cache file paths (dns.cache, whois.cache, idb.cache) work on Linux"
    - "No hardcoded backslash path separators remain anywhere in the helpers/ directory"
  artifacts:
    - path: "helpers/helpers.go"
      provides: "Cross-platform tar extraction and output combination paths"
      contains: "filepath.Join"
  key_links:
    - from: "helpers/helpers.go ExtractTarGz"
      to: "filepath.Join"
      via: "tar entry path construction"
      pattern: "filepath\\.Join\\(dir"
    - from: "helpers/helpers.go CombineOutputs"
      to: "filepath.Join"
      via: "combined output file path"
      pattern: "filepath\\.Join\\(k"
---

<objective>
Replace hardcoded backslash path separators in helpers/helpers.go (ExtractTarGz and CombineOutputs), remove resolved TODO comments, and perform a final sweep of all helpers/*.go files to confirm zero backslash path separators remain.

Purpose: The ExtractTarGz function (used by MaxMind DB extraction) and CombineOutputs function both use fmt.Sprintf with hardcoded backslashes for path construction. Fixing these completes the helpers/ directory cross-platform work. The cache files (dns.cache, whois.cache, idb.cache) are bare filenames in vars/vars.go with no directory path separators, so they already work cross-platform -- the sweep task will confirm this.

Output: Modified helpers/helpers.go with zero hardcoded backslash path separators. Verified clean sweep of entire helpers/ directory.
</objective>

<execution_context>
@C:\Users\Joe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Joe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@helpers/helpers.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace backslash paths in helpers.go with filepath.Join</name>
  <files>helpers/helpers.go</files>
  <action>
Replace all fmt.Sprintf path constructions using hardcoded backslashes with filepath.Join() calls. The file already imports "path/filepath". Specific changes:

1. Line 155 (ExtractTarGz -- tar directory entry):
   Change: `targetDir := fmt.Sprintf("%v\\%v", dir, header.Name)`
   To: `targetDir := filepath.Join(dir, header.Name)`

2. Line 166 (ExtractTarGz -- tar file entry):
   Change: `targetDir := fmt.Sprintf("%v\\%v", dir, header.Name)`
   To: `targetDir := filepath.Join(dir, header.Name)`

3. Line 790 (CombineOutputs -- combined output file):
   Change: `tmpCombinedOutput := fmt.Sprintf("%v\\combinedOutput_%v.csv", k, t)`
   To: `tmpCombinedOutput := filepath.Join(k, fmt.Sprintf("combinedOutput_%v.csv", t))`

4. Remove the "TODO - Support Cross-Platform Compilation" comments on lines 154 and 165 -- the work they reference is now complete.

Note: The "fmt" import is still needed elsewhere in the file, do NOT remove it. The "path/filepath" import already exists.
  </action>
  <verify>
Run: `grep -n "\\\\" helpers/helpers.go` -- should return zero matches (no hardcoded backslashes remain).
Run: `grep -n "TODO.*Cross-Platform" helpers/helpers.go` -- should return zero matches.
Run: `CGO_ENABLED=0 go build .` -- should compile successfully.
  </verify>
  <done>All 3 backslash path constructions in helpers.go replaced with filepath.Join(). Both cross-platform TODO comments removed. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Final sweep of all helpers/*.go for remaining backslash path issues</name>
  <files>helpers/maxmind.go, helpers/threatIntel.go, helpers/helpers.go, helpers/whois.go, helpers/network.go, helpers/ipNetGen.go</files>
  <action>
Perform a comprehensive grep sweep of all .go files in the helpers/ directory to confirm zero hardcoded backslash path separators remain. This is the final verification task for Phase 2.

1. Run: `grep -rn "\\\\" helpers/*.go` to find any remaining backslash literals in Go string contexts.
   - If any results are found that represent path separators, fix them with filepath.Join().
   - Note: backslashes in regex patterns or escape sequences (like `\n`, `\t`, `\"`) are NOT path separators and should be left alone.

2. Run: `grep -rn "TODO.*Cross-Platform\|TODO.*cross-platform" helpers/*.go` to confirm all cross-platform TODOs are resolved.

3. Run: `CGO_ENABLED=0 go build .` to confirm the full project builds successfully after all Phase 2 changes.

4. Verify cache file paths: Confirm that vars/vars.go uses bare filenames ("dns.cache", "whois.cache", "idb.cache") without directory separators -- these work cross-platform as-is via fastcache.LoadFromFileOrNew() and SaveToFile().

If plan 02-01 has not yet executed (these files may not be fixed yet), this task should only verify helpers.go changes from Task 1 and note any remaining issues from maxmind.go/threatIntel.go for tracking.
  </action>
  <verify>
Run: `grep -rn "\\\\" helpers/*.go` -- any matches should only be non-path backslashes (regex, escape sequences). Zero path separator backslashes.
Run: `grep -rn "TODO.*Cross-Platform" helpers/*.go` -- should return zero matches.
Run: `CGO_ENABLED=0 go build .` -- compiles successfully.
  </verify>
  <done>Zero hardcoded backslash path separators in any helpers/*.go file. Zero unresolved cross-platform TODO comments. Full project builds. DBPATH-04 confirmed (cache files use bare filenames, no path separators needed).</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn "\\\\" helpers/*.go` returns only non-path backslashes (or zero results)
2. `grep -rn "TODO.*Cross-Platform" helpers/*.go` returns zero results
3. `CGO_ENABLED=0 go build .` compiles successfully
4. Requirement DBPATH-04 confirmed satisfied (cache files are bare filenames)
</verification>

<success_criteria>
- Zero hardcoded backslash path separators in helpers/helpers.go
- Zero cross-platform TODO comments in helpers/helpers.go
- Full helpers/ directory sweep clean
- Build compiles with CGO_ENABLED=0
- Cache files confirmed cross-platform (bare filenames, no path construction needed)
</success_criteria>

<output>
After completion, create `.planning/phases/02-helper-path-fixes/02-02-SUMMARY.md`
</output>
